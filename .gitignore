# The Debug directory contains compiled object file and its intermediate
Debug/

# The Debug.launch file is associated with debugging and thus not included
*Debug.launch

# Linker script is automatically generated by STM32CubeIDE
# Since don't know if exists another linker script elsewhere, only excludes
# the one in the project root
/*.ld
